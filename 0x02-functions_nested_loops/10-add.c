#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @x: the first integer to add
 * @y: the second integer to add
 *
 * Return: the result for x and y
 */

int add(int x, int y);
{
	return (x + y);
}

